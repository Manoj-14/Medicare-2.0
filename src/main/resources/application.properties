spring.profiles.active=dev

spring.datasource.url = ${DB_URL}
spring.datasource.username = ${DB_USER}
spring.datasource.password = ${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto= update
spring.jpa.show-sql=true

spring.graphql.graphiql.enabled=true

#spring.security.oauth2.client.registration.facebook.client-i   d=${FACEBOOK_CLIENT_ID}
#spring.security.oauth2.client.registration.facebook.client-secret=${FACEBOOK_SECRET_KEY}
#
#spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
#spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_SECRET_KEY}

spring.security.user.name= user
spring.security.user.password= test

app.jwtsecret= ${JWT_SECRET_KEY}

app.aws.accessKey = ${AWS_ACCESS_KEY}
app.aws.secretKey= ${AWS_SECRET_KEY}


app.aws.endpoint = ${AWS_ENDPOINT}
app.aws.bucketName = ${AWS_BUCKET_NAME}

server.port=8088